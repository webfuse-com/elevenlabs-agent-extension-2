{
  "private": "true",
  "main": "background.js",
  "scripts": {
    "build": "esbuild extension/popup.js --outfile=extension/_popup.js --bundle --minify",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  },
  "dependencies": {
    "@elevenlabs/client": "^0.8.0"
  }
}
